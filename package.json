{
  "name": "bestsoftguide",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "imports": {
    "#internal/nuxt/*": "./.nuxt/dist/server/*"
  },
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate --verbose",
    "generate:static": "nuxt generate --force --verbose",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "npm run generate:static && echo 'Build complete! Upload .output/public/ to Hostinger'"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "nuxt": "^3.8.0"
  },
  "dependencies": {
    "@headlessui/vue": "^1.7.16",
    "@heroicons/vue": "^2.0.18",
    "@nuxt/content": "^3.6.3",
    "@nuxtjs/tailwindcss": "^6.8.4",
    "better-sqlite3": "^8.7.0"
  }
}
