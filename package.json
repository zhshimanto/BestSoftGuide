{
  "name": "bestsoftguide",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "npm run generate && echo 'Build complete! Upload .output/public/ to Hostinger'"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "nuxt": "^3.8.0"
  },
  "dependencies": {
    "@nuxtjs/tailwindcss": "^6.8.4",
    "@headlessui/vue": "^1.7.16",
    "@heroicons/vue": "^2.0.18"
  }
}
